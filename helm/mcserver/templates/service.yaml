apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcserver.fullname" . }}
  labels:
    {{- include "mcserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcserver.selectorLabels" . | nindent 4 }}
